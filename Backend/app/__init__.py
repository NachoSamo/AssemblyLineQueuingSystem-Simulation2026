"""Paquete raíz del backend de la simulación del Ejercicio 135.

Contiene la aplicación FastAPI (`main`), su configuración de servicio (`config`)
y las capas `controllers`, `services`, `models` y `utils`.
"""

__version__ = "1.0.0"
